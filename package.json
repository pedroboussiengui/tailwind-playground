{
  "name": "tailwind_project",
  "version": "1.0.0",
  "description": "project to train tailwind",
  "main": "index.js",
  "scripts": {
    "tw": "npx tailwindcss -i ./src/input.css -o ./src/output.css",
    "start": "npm run tw && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.7",
    "tailwindcss": "^3.4.14"
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
